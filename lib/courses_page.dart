
import 'package:flutter/material.dart';
import 'styles.dart';

class CoursesPage extends StatelessWidget {
  const CoursesPage({super.key});

  @override
  Widget build(BuildContext context) {
      return Center(
        child: Text('Courses Page', style: kPageTitleTextStyle),
      );
    }
  }
